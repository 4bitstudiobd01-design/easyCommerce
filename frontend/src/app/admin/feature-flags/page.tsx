'use client';

import React from 'react';
import { PlaceholderPage } from '@/components/dashboard/common/PlaceholderPage';

export default function FeatureFlagsPage() {
  return (
    <PlaceholderPage
      title="Feature Flags"
      section="System"
      description="Enable, disable, and canary roll out beta features across merchant clusters."
    />
  );
}
