'use client';

import React from 'react';
import { PlaceholderPage } from '@/components/dashboard/common/PlaceholderPage';

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      section="Operations"
      description="View system alerts, incident notifications, webhook failure logs, and broadcast announcements."
    />
  );
}
